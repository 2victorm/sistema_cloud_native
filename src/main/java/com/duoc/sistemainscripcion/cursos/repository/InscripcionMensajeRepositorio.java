package com.duoc.sistemainscripcion.cursos.repository;

import com.duoc.sistemainscripcion.cursos.model.InscripcionMensaje;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InscripcionMensajeRepositorio extends JpaRepository<InscripcionMensaje, Long> {
}
